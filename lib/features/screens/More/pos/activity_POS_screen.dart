import 'package:flutter/material.dart';

class ActivityScreenPOS extends StatelessWidget {
  const ActivityScreenPOS({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
