import 'package:flutter/material.dart';

class CheckoutScreenPOS extends StatelessWidget {
  const CheckoutScreenPOS({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
